//Async anonymous functions

setTimeout(() => console.log(`🐇 finishes!`), 1000);

const consoleLogger = (animal) => console.log(`${animal} finishes!`)

consoleLogger(`🐢 finishes!`);

//Non-blocking code
 